module

public import UpperBound.PotentialDecrease.OneStep
public import UpperBound.PotentialDecrease.StableInterval
public import UpperBound.PotentialDecrease.Drift

/-! # Potential decrease (aggregator)

This module was split into `UpperBound/PotentialDecrease/`:
`Helpers`, `OneStep`, `StableInterval`, and `Drift`. It re-exports them so existing
`import UpperBound.PotentialDecrease` clients keep working. -/
