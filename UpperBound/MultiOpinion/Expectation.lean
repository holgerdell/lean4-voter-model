module

public import UpperBound.MultiOpinion.Expectation.GeomTail
public import UpperBound.MultiOpinion.Expectation.Metaphase
public import UpperBound.MultiOpinion.Expectation.OneBlock
public import UpperBound.MultiOpinion.Expectation.Rbeta

/-! # `E[R_β]` bound (aggregator)

This module was split into `UpperBound/MultiOpinion/Expectation/`:
`GeomTail`, `Metaphase`, `OneBlock`, and `Rbeta`. It re-exports them so existing
`import UpperBound.MultiOpinion.Expectation` clients keep working. -/
